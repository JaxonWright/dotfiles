# System tray

Cloned from `omarchy.tray`.

Source: `/usr/share/omarchy/shell/plugins/bar/widgets/Tray.qml`

The original remains built into Omarchy. Remove this plugin directory or swap
your bar entry back to `omarchy.tray` whenever you want to return to the built-in.

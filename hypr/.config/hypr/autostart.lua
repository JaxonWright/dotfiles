-- Extra autostart processes.
-- o.launch_on_start("my-service")

-- Logitech receiver (restored from the pre-Quattro autostart.conf).
o.launch_on_start("solaar --window=hide")

-- Launch Discord and Steam on boot.
o.launch_on_start("discord")
o.launch_on_start("steam")
